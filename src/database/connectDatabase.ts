export async function connectDatabase() {
    try {
        // TODO: Connect database
    } catch (error) {
        console.error(error);
        process.exit(1);
    }

    console.info(`••••• Connect database success.`);
}